import 'package:flutter/material.dart';

/// login weight
/// check login status and login with name password
class AccountLoginWidget extends StatelessWidget {

  final String preToken;

  AccountLoginWidget({Key key, this.preToken}) :super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("ok");
  }
}

